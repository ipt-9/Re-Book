# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=bmsd22a_rebook
DB_PORT=3306  # Optional, default MySQL port

# App Environment
APP_ENV=development  # Change to 'production' in live environment
APP_DEBUG=true  # Set to false in production for security

# Site URL (useful for building absolute links)
APP_URL=http://localhost

# Security Keys (if your app uses encryption)
SECRET_KEY=your_random_secret_key_here

# Mail Server (if sending emails)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=your_email@example.com
MAIL_PASSWORD=your_email_password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=no-reply@example.com
MAIL_FROM_NAME="Your App Name"
